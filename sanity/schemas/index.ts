import { product } from "./product";
import { siteSettings } from "./siteSettings";
import { heroSection } from "./heroSection";
import { testimonial } from "./testimonial";
import { page } from "./page";
import { reviewSubmission, order, emailEvent, messageCampaign, broadcastSettings } from "./commerce";

export const schemaTypes = [
  product,
  siteSettings,
  heroSection,
  testimonial,
  page,
  reviewSubmission,
  order,
  emailEvent,
  messageCampaign,
  broadcastSettings,
];
