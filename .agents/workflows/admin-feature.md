---
title: Admin Feature
description: Build one reliable VoltGear merchant/admin workflow.
---

Use `voltgear-feature` + `admin-commerce` and `supabase-safe-change` if schema/data-layer work is required.

Verify create/edit/validation/order/visibility/reload persistence/public consequence/auth. Do not delete unrelated records. Stop after final verification.
